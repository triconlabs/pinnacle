import { moduleFor, test } from 'ember-qunit';

moduleFor('view:paper-shadow', 'Unit | View | paper shadow');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
