import { ifAnswer } from '../../../helpers/if-answer';
import { module, test } from 'qunit';

module('Unit | Helper | if answer');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = ifAnswer(42);
  assert.ok(result);
});
